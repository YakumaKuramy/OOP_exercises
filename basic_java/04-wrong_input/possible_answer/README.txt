Run the command " cat test.txt | java Main " and
Run the command " cat test.txt | java Error " to
run the programs without having to type any input.

The code that works is on the "Main.java" file.

The programs where the error occurs, have it
because in the input reading, from the scanner,
it is taking an incomplete line.
